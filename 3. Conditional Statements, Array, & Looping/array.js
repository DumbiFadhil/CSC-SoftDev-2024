// Contoh: Membuat dan mengoperasikan array
let fruits = ['apple', 'banana', 'orange'];
console.log(fruits.length); // Panjang array
console.log(fruits.join(', ')); // Mengubah array menjadi string

// Contoh: String menjadi Array
let pisang = "Pisang";
console.log(pisang.length);
console.log(pisang.split(""));
